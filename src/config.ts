/**
 * Url where the api is located
 */

export const apiUrl: string = process.env.API_URL || "";

/**
 * Url where the website is located
 */

export const webUrl: string = process.env.WEBSITE_URL || "";
